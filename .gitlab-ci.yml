image: alpine:latest

stages:
    - build

build:
    stage: build
    script:
        - echo "hello:"
        - ls jil/dev | head -n 1 ##latest files
        - ls -lrt ##list all directory
        - ls -l | wc -l ##list number of files
        - ls jil/dev | wc -l ##list number of files in jil folder
        - ls jil/dev ##list all files in the directory
        - find jil/dev -name "*.jil" | wc -l ##finding number of files with specific extension 
        - find jil/dev -name "*.jil"
        - find jil/qa -name "*.jil" | wc -l
        - find jil/prod -name "*.sh" | wc -l
        - FILES=$(find jil/dev -name "*.jil" | wc -l) ##storing the values in variable 
        - echo "There are $FILES in dev env." 
        - |
            if [ $FILES == 2 ]
            then
                echo "Sunday is the holiday"
            else
                echo "wrong cmd"
            fi
        - | 
            abc="sample1.jil"
            for dirname in $(ls -d jil/dev/*)  ##for loop
            do
                echo "$dirname"
                if cmp -s "$dirname" "jil/dev/$abc"; ##comparing files
                then
                    echo "file found"
                else 
                    echo "file not found"
                fi
            done
        - find jil/dev/* -iname "*.jil" -mtime -1
        - find jil/dev/* -iname "*.jil" -mtime -4
        - find jil/dev/* -iname "*.jil" -mtime 1
        - find jil/dev/* -iname "*.jil" -mtime 3
        - find jil/dev/* -iname "*.jil" -mtime 4
        - find jil/prod/* -iname "*.jil" -mtime -1
        - find jil/prod/* -iname "*.jil" -mtime -4
        - find jil/prod/* -iname "*.jil" -mtime 1
        - find jil/prod/* -iname "*.jil" -mtime 3
        - find jil/prod/* -iname "*.jil" -mtime 4
        - find jil/prod/* -maxdepth 1 -mtime -1
        - find jil/prod/* -maxdepth 1 -mtime -4
        ##- find jil/dev -type f -newermt 4/19/2020 ##does not work in the yml file becoz of newermt
        ##- find jil/dev -type f -newerat 4/19/2020 ##does not work in the yml file becoz of newerat
        
        - rm jil/dev/readme.md
        - echo "removed successfully"